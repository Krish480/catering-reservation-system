<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Product Form</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen p-6">
  <div class="max-w-3xl mx-auto bg-white p-6 rounded shadow">
    <h2 class="text-xl mb-4"><%= product ? 'Edit Product' : 'Add Product' %></h2>

    <form action="<%= action %>" method="POST" enctype="multipart/form-data" class="space-y-4">
      <div>
        <label class="block text-sm">Name</label>
        <input name="name" value="<%= product ? product.name : '' %>" required class="w-full border px-3 py-2 rounded"/>
      </div>

      <div class="grid grid-cols-2 gap-4">
        <div>
          <label class="block text-sm">Price</label>
          <input name="price" value="<%= product ? product.price : '' %>" required class="w-full border px-3 py-2 rounded"/>
        </div>
        <div>
          <label class="block text-sm">Category</label>
          <input name="category" value="<%= product ? product.category : '' %>" class="w-full border px-3 py-2 rounded"/>
        </div>
      </div>

      <div>
        <label class="block text-sm">Offer (e.g. 10% OFF)</label>
        <input name="offer" value="<%= product ? product.offer : '' %>" class="w-full border px-3 py-2 rounded"/>
      </div>

      <div>
        <label class="block text-sm">Description</label>
        <textarea name="description" class="w-full border px-3 py-2 rounded"><%= product ? product.description : '' %></textarea>
      </div>

      <div>
        <label class="block text-sm">Image</label>
        <input type="file" name="image" accept="image/*" class="block"/>
        <% if(product && product.imageUrl){ %>
          <img src="<%= product.imageUrl %>" class="w-32 h-24 object-cover mt-2 rounded"/>
        <% } %>
      </div>

      <div class="flex gap-3">
        <button class="bg-orange-600 text-white px-4 py-2 rounded">Save</button>
        <a href="/admin/products" class="px-4 py-2 border rounded">Cancel</a>
      </div>
    </form>
  </div>
</body>
</html>
